# Procurações (POA) — validade operacional
## 12/09/2026 — tick diurno (~14:00)

### Dor
Advogado descobre procuração vencida só na audiência ou ao protocolar peça.

### Entrega
- Modelo `PowerOfAttorney` (active / expired / revoked)
- API JWT: `GET/POST /poa`, `GET /poa/expiring`, `POST /poa/{id}/revoke`
- Auto-marca `expired` quando `expires_at` passou (flag operacional, não cartório)
- UI `/dashboard/poa` + link em Operações → Casos
- Testes `test_poa_routes.py` + guard `poa_router` + check no QA simulator

### Limite
Não é registro cartorial nem opinião sobre poderes/substabelecimento.
