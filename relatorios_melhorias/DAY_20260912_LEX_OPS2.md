# DAY_20260912 — Lex professional polish (ops modules) tick 12

Data: 12 de setembro de 2026 (daytime polish)

## Objetivo

Estender o domínio `escritorio_ops` (marcadores + addendum) e o playbook interno para que a Lex cite, **quando relevante**, os módulos operacionais do produto — sem inventar norma e **sem** tocar no bloco `include_router` de `main.py`.

## A) Domínio `escritorio_ops`

Arquivo: `backend/ai/professional_domains.py`

### Marcadores adicionados (detecção)

| Tema | Exemplos de markers |
| --- | --- |
| Trust / valores de clientes | `trust accounting`, `conta trust`, `valores de clientes`, `conta de custodia`, `custodia de valores` |
| E-sign | `e-sign`, `esign`, `assinatura eletronica`, `envelope de assinatura` |
| Monitor de intimações | `monitor de intimacoes`, `monitor de processos`, `intimacoes no monitor` |
| Aging / honorários em atraso | `aging`, `aging de recebiveis`, `honorarios em atraso`, `recebiveis em atraso` |
| NFS-e stub | `nfse stub`, `nfs-e stub`, `nfs-e` |
| Consentimento WhatsApp LGPD | `consentimento whatsapp`, `whatsapp lgpd`, `consentimento lgpd whatsapp` |

### Addendum

O `ADDENDUM OPERACAO DO ESCRITORIO (PRODUTO)` agora orienta citar, quando couber:

- trust / valores de clientes (custódia)
- e-sign / assinatura eletrônica (**stub**)
- monitor de intimações (**stub local — não é DJEn/tribunal real**)
- aging / honorários em atraso
- NFS-e stub (**sem prefeitura**)
- consentimento WhatsApp (LGPD) antes de outbound

Termos exigidos pelo eval offline (`prazos`, `acervo`, `documentos recuperados`, `Nao invente clausulas`) foram **preservados**.

## B) Playbook (injeção no orchestrator)

Arquivo: `backend/ai/knowledge_packs/professional_playbook.md`

- Parágrafo curto sob **Postura** (cabe no truncate de 2000 chars usado por `load_professional_playbook(2000)` / `LegalAIOrchestrator`).
- Seção **Operacoes de escritorio** ampliada com o mesmo checklist de módulos.

`GET /operations/shortcuts` já lista Trust, E-Sign, Monitor, Financeiro etc. — sem alteração de rotas neste tick.

## C) Eval

```bash
cd backend
.\venv311\Scripts\python.exe scripts\run_professional_domain_eval.py
```

| Resultado | Valor |
| --- | --- |
| Cases | **14/14** |
| `pass_rate` | `1.0` |
| Modo | Offline (só detectores + addenda; sem LLM) |

## Fora de escopo / guardrails

- **Não** editado `main.py` `include_router`.
- **Não** inventada norma legal.
- **Não** commit (pedido explícito).
- Nenhum wipe de arquivos.

## Arquivos tocados

- `backend/ai/professional_domains.py`
- `backend/ai/knowledge_packs/professional_playbook.md`
- `DAY_20260912_LEX_OPS2.md` (este)
